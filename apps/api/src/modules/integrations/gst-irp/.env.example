# GST IRP Configuration

# Environment (sandbox or production)
GST_IRP_ENVIRONMENT=sandbox

# GSP Credentials
GST_IRP_GSTIN=29AABCT1332L000
GST_IRP_USERNAME=your_username
GST_IRP_PASSWORD=your_password
GST_IRP_CLIENT_ID=your_client_id
GST_IRP_CLIENT_SECRET=your_client_secret

# Optional: Custom GSP API URL (if using specific GSP provider)
# GST_IRP_API_URL=https://gsp.adaequare.com/test/enriched/ei/api

# Optional: Digital Signature Certificate
# GST_IRP_CERTIFICATE_PATH=/path/to/certificate.p12
# GST_IRP_CERTIFICATE_PASSWORD=cert_password

# Optional: Timeout and Retry Settings
# GST_IRP_TIMEOUT=30000
# GST_IRP_MAX_RETRIES=3
